apiVersion: v1 
Kind: Service
metadata: 
  name: nginx-service
spec: 
  selector: 
    project: expense
    module: backend
    environment: dev
  ports:
   - name: port-name
     protocol: TCP
     port: 80 # service port
     targetPort: 80 # container port 